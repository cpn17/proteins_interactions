[project]
name = "proteins-contacts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cpn17", email = "nhung.caophuong@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "biopython>=1.88",
    "openbabel>=3.2.1",
]

[project.scripts]
proteins-contacts = "proteins_contacts:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
